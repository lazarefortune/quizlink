/**
 * Dashboard section: auth and shell are provided by (app)/layout.
 */
export default function DashboardSectionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
