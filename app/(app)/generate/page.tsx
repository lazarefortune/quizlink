"use client";

import { GeneratePage } from "./page-content";

/**
 * Generate (IA) page. Auth is enforced by (app)/layout.
 */
export default function GeneratePageWrapper() {
  return <GeneratePage />;
}
